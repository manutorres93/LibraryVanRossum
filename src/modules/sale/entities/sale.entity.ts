import { Column, Entity, PrimaryGeneratedColumn } from 'typeorm';

//@Entity({ name: 'authors' })

export class Sale {
  /* @PrimaryGeneratedColumn()
  id: string;
  
  @Column()
  name: string;

  @Column()
  pages: number; */
}
